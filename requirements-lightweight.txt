# Lightweight requirements for Streamlit Cloud deployment
# Uses API-based embeddings instead of heavy local ML models

# Core application
streamlit
pandas
numpy
plotly
joblib
xgboost
imbalanced-learn
requests

# Gemini API for LLM and embeddings
google-generativeai==0.3.2

# Lightweight RAG components
qdrant-client==1.7.0
rank-bm25
PyPDF2==3.0.1

# Total package size: ~100MB (vs. 1.8GB with LangChain + PyTorch)
